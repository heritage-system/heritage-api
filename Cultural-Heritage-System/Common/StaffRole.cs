namespace Cultural_Heritage_System.Common
{
    public enum StaffRole
    {
        CONTENT_REVIEWER,   // Duyệt & chỉnh sửa di sản, bài viết
        EVENT_MANAGER,      // Quản lý, tổ chức sự kiện lễ hội
        SUPPORT_STAFF,      // Hỗ trợ xử lý báo cáo, hỗ trợ cộng đồng
        COORDINATOR,        // Phân công contributor, theo dõi tiến độ
        MODERATOR,          // Kiểm duyệt bình luận, review
        ADMIN_ASSISTANT     // Hỗ trợ admin quản lý hệ thống
    }
}
